---
title: ScopeQL reference
---

ScopeQL is ScopeDB's pipelined relational language for querying data, loading rows, and defining tables and indexes.

Use this section when you need exact ScopeQL syntax. Use [How to use this reference](/reference/how-to-use-this-reference) first if you need the syntax notation and example conventions.

### [Query syntax](/reference/commands/stmt-query)

Read the ScopeQL query pipeline: `FROM`, `VALUES`, `WHERE`, `SELECT`, `AGGREGATE`, `ORDER BY`, `LIMIT`, and related clauses.

### [Query operators](/reference/commands/query-operators)

Look up arithmetic, comparison, logical, and set operators.

### [Statements](/reference/commands/statements)

Look up DDL and DML statements such as `CREATE TABLE`, `INSERT`, and `CREATE INDEX`.

### [DDL statements](/reference/commands/stmt-ddl)

Look up table, schema, database, view, and index definition statements.

### [DML statements](/reference/commands/stmt-dml)

Look up data modification statements.
